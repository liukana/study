<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/ivThemeImage"
        android:layout_width="330dp"
        android:layout_height="165dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:background="#0ff"/>

    <FrameLayout
        android:id="@+id/flLabelLayout"
        android:layout_width="42dp"
        android:layout_height="52dp"
        android:layout_alignRight="@id/ivThemeImage"
        android:layout_alignTop="@id/ivThemeImage"
        android:layout_marginRight="24dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/theme_recommend_label" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="4dp"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvThemeRecommendNum"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="-2dp"
                android:includeFontPadding="false"
                android:textColor="#ffffff"
                android:textSize="22sp"
                android:textStyle="bold"
                android:text="32" />

            <TextView
                android:id="@+id/tvLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:includeFontPadding="false"
                android:text="精选"
                android:textColor="#ffffff"
                android:textSize="10sp" />
        </LinearLayout>
    </FrameLayout>

</RelativeLayout>